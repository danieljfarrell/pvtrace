"""pvtrace studio: a local web application for building scenes, running
simulations and exploring results.

Start it with::

    pvtrace-cli studio scene.yml

Requires the optional studio dependencies (``pip install fastapi
uvicorn``) and the compiled engine (``python -m pvtrace.engine.build``).
"""
